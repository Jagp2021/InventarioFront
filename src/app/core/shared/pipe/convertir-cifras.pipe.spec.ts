import { ConvertirCifrasPipe } from './convertir-cifras.pipe';

describe('ConvertirCifrasPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertirCifrasPipe();
    expect(pipe).toBeTruthy();
  });
});
