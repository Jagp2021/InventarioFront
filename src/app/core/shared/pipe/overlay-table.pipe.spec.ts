import { OverlayTablePipe } from './overlay-table.pipe';

describe('OverlayTablePipe', () => {
  it('create an instance', () => {
    const pipe = new OverlayTablePipe();
    expect(pipe).toBeTruthy();
  });
});
