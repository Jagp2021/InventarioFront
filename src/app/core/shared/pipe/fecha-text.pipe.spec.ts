import { FechaTextPipe } from './fecha-text.pipe';

describe('FechaTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FechaTextPipe();
    expect(pipe).toBeTruthy();
  });
});
